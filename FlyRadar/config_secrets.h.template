#pragma once
// ============================================================================
// FlyRadar — secrets / credentials  (TEMPLATE)
//
// 1. Copy this file:  cp config_secrets.h.template config_secrets.h
// 2. Fill in your values in config_secrets.h
// 3. config_secrets.h is in .gitignore — it will never be committed
//
// WiFi note: credentials here are only a fallback for the first boot.
// On first run the device opens WiFi AP "FlyRadar-Setup" — configure
// from any browser at 192.168.4.1. Credentials are then saved to flash.
// ============================================================================

#define WIFI_SSID           "YourNetworkName"
#define WIFI_PASSWORD       "YourPassword"

// Open-Meteo is used by default (free, no key needed).
// Only fill this if you switch to OpenWeatherMap.
#define OPENWEATHER_API_KEY ""

#define ADSBDB_API_KEY      ""
